import { createSlice } from "@reduxjs/toolkit";

const initialState = {};

export const chrono = createSlice({
  name: "chrono",
  initialState,
  reducers: {},
});

// export const {  } = chrono.actions;
export default chrono.reducer;
